# GitHub 公开发布审查

## 2026-09-12 源码上传隐私复核

- 上传仅包含应用源码、开发文档、构建脚本、许可证及 15 道功能演示题；不包含正式考试题库、个人原始文档、数据库、作答记录、会话配置、签名密钥或既有发行包。
- 移除历史文档中的个人云项目标识和维护者工作区绝对路径；补充私人文件与压缩包忽略规则，仅允许内置演示 ZIP。
- 候选文件及演示 ZIP 内容已进行本地模式扫描；演示 ZIP 题目与仓库源文件逐字节一致。图标中的生成工具证书联系地址经人工复核，不属于维护者账号。
- 本次静态分析通过；首次全量测试出现一项本地服务闭环失败，该文件单独复测通过，随后全量复跑 119 项全部通过。未据此修改业务代码或测试断言。
- Git 提交使用 GitHub 隐私邮箱。上传到既有仓库时保留其历史及私有可见性，不自动公开既有仓库。
- 此记录只证明源码上传前的本地检查，不代表本次完成发行包构建、真实纸张、Android 真机或真实云同步验收。

## 2026-09-12 当前补充

- 同步后端三份迁移原本已在源码中。本次扩充 [部署指南](../supabase/README.md)，增加纯 Python SQL 合并工具和只读结构检查 SQL。生成器仅处理本地文件，不连接云端。
- 上游 OMR 源码虽已在工作树删除，却仍留在初始索引，本次已清理旧目录的暂存记录。当前 lib/test/tool/CI 无旧项目名匹配；接口使用 WindowsOmrScanner，新事件使用 paper_omr_scan，已存事件不重写。历史文档保留来源证据。
- 自有代码采用 [MIT](../LICENSE)，理由与分发范围见 [许可说明](licensing.md)。第三方声明区分 OpenCV Python 包与 OpenCV 本体，增加原始依赖许可收集；当前环境实际收集 49 份许可文本（9 个 wheel 分发包与 Python）。
- Flutter analyze 无问题；全量 Flutter 测试 108 项通过；Python 阅卷契约/合成图测试 9 项通过。PowerShell 发布脚本解析通过且保留 UTF-8 BOM。
- 本次未部署新云项目、未重新生成完整 Windows/Android 发行包、未做真实纸张或 Android 真机验收；没有 Git commit、push 或 GitHub Release。
- 首次公开发布需完整 clean 构建，携带 LICENSE、第三方声明和原始依赖许可，不能使用旧 dist 包。云端需按部署指南做新项目同步与跨账号验收。

下文为 2026-09-11 历史记录，其中“保留旧 OMR 源码”“许可证待定”及旧 CI 描述已被上述状态取代，不作为当前发布结论。

审查日期：2026-09-11。范围为本仓库当前工作树、发布脚本、本地测试、同步 SQL 静态检查和拟提交文件；不是对所有代码路径的完整安全审计。

## 发现及处理

| 优先级 | 发现 | 处理结果 |
| --- | --- | --- |
| P1 | 导入器将外部 `bank_id` 拼入媒体目录，`../`、反斜杠或绝对路径值可改变写入位置；远端媒体落盘存在相同入口 | 新增共享目录组件校验；ZIP、TSV/CSV 预览、导入提交、远端媒体接收和落盘均阻止危险 ID；添加隔离回归测试 |
| P1 | 暂存区落后于工作树，仍包含已删除的 Android OMR 等旧代码 | 准备首次提交时同步索引到当前文件；不恢复旧文件 |
| P2 | 新安装默认使用维护者个人 Supabase 项目 | 删除硬编码默认连接，使用可选 dart-define 或设置页配置；已保存配置仍优先生效 |
| P2 | Windows 发布脚本未执行文档声称的 analyze 关卡 | 正常发布流程补上 analyze；显式 SkipTests 仍跳过检查 |
| P2 | PDF 集成测试假定 Poppler 页码文件名，实际生成后仍报找不到文件 | 使用 `-singlefile` 确定输出名；全量测试中 PDF 实际生成及渲染通过 |
| P2 | 仓库缺少供其他使用者阅读的 README 和 CI | 新增 README、GitHub Windows 分析/测试/编译工作流；补齐数据、凭据和产物忽略规则及换行约定 |

Publishable Key 本身不是管理密钥，删除个人默认配置的目的是让公开使用者连接自己的服务。API key 的权限说明见 [Supabase 官方文档](https://supabase.com/docs/guides/getting-started/api-keys)。

## 本次验证

- `flutter analyze`：无问题。
- `flutter test --reporter expanded`：90 项通过，包括 PDF 集成测试；本次无跳过。
- Python OMR bridge 几何契约：1 项通过。此测试替身不验证真实图像识别。
- `flutter build windows --release`：本机编译成功；本次没有重打 OMR sidecar 或生成新的 dist 发行包。
- PowerShell 发布脚本：解析无错误，UTF-8 BOM 保留。
- 拟提交文件扫描：未匹配到所检查的 Secret Key、GitHub token、JWT、私钥模式；未发现大于 50 MiB 的候选文件。模式扫描不等于完整凭据审计。
- 首次提交暂存区：241 个文件，已与工作树同步；无嵌套 Git 子模块占位，OMRChecker 源码和 LICENSE 已纳入，已删除的旧 Android OMR 文件不在索引中。索引密钥模式扫描无命中。
- 忽略规则验证：`.env`、SQLite 数据库、Android keystore、dist 产物被忽略，内置测试 ZIP 可跟踪。
- SQL 静态检查：同步表启用 RLS，所有权条件使用 `auth.uid() = user_id`，RPC 为 security invoker；未连接真实云端核实实际部署。

本次未进行 Android 真机、真实纸张 OMR、真实云同步、Android 正式签名或 GitHub 托管 CI 验收。历史验证文档中的旧设备和云部署记录不能代替这些验收。

## 公开前待办

1. **许可证暂不决定**：维护者已选择保留待办。项目自有代码不声明开源授权；第三方 OMRChecker 继续保留原 GPL-3.0 许可。正式分发前核对 bridge 和全部第三方二进制的许可材料。
2. **目标仓库及提交身份**：当前无远端、无提交；未配置可用 Git 作者姓名/邮箱。提供目标 GitHub 仓库及作者信息后才能完成首次提交和推送。不要使用猜测的身份。
3. **Android 发布签名**：仍为 debug 签名，不应当作正式发行 APK。
4. **CI 首次运行**：工作流已准备，但只有推送到 GitHub 后才能验证托管环境；当前 CI 不打包 OMR sidecar，也不自动发布 Release。Poppler 未安装的 runner 会跳过 PDF 集成测试。
5. **干净克隆构建**：README 给出通用 Flutter 命令，维护者 `.bat` 与 Android PowerShell 脚本仍依赖本机目录。其他机器需安装工具链；本次不宣称已完成异机或全新克隆构建。

## 首次上传范围

仅上传 `personal_exam_app/` 这个 Git 仓库。不要将上级目录的正式题库、个人交接材料、SDK、运行数据库或已有 dist 文件加入仓库。当前更改在本地准备，不触发远端创建、推送、云迁移或发布。
